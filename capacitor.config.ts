import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: 'in.caremizhi.app',
  appName: 'CareMizhi',
  webDir: 'dist',
  backgroundColor: '#f2f7fc',
  android: { backgroundColor: '#f2f7fc' },
};
export default config;
